# CI-only dependencies: subset of requirements.txt for unit tests.
# Excludes packages never imported in src/ and server-only packages.
#
# Guarded imports (try/except) are KEPT because tests exercise those
# code paths at runtime even though collection doesn't require them.

# Scientific Computing
numpy>=1.23.0
pandas>=1.4.0
scipy>=1.9.0

# ML (hard-imported at module level)
# torch installed separately via --index-url for CPU-only wheel
sentence-transformers>=2.2.0,<3.0.0
transformers>=4.30.0,<5.0.0
tokenizers>=0.13.0

# ML Operations
mlflow>=2.9.0,<3.0.0

# Vector Search
faiss-cpu>=1.7.4
weaviate-client>=4.15.0,<5.0.0

# Document Processing
PyMuPDF>=1.23.0
pdfplumber>=0.10.0

# Text Processing
nltk>=3.8.0
scikit-learn>=1.3.0

# NLP
spacy>=3.7.0,<3.8.0

# Utilities
python-dotenv>=1.0.0
typing-extensions>=4.4.0
pydantic>=2.0.0,<3.0.0
pyyaml>=6.0.0
psutil>=5.9.0
rank-bm25>=0.2.2
requests>=2.31.0
tqdm>=4.65.0
tenacity>=8.0.0
huggingface-hub>=0.16.0

# Graph Processing
networkx>=3.0,<4.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.17.0,<6.0.0

# LLM Adapters (guarded imports, but tests exercise these paths)
anthropic>=0.8.0
openai>=1.0.0
mistralai>=0.4.0
tiktoken>=0.5.0

# Agent System (guarded imports, but agent tests exercise these paths)
langchain>=0.1.0
langchain-community>=0.1.0
langchain-core>=0.1.0
langchain-openai>=0.0.5

# Testing
pytest>=7.4.0,<8.0.0
pytest-cov>=4.1.0
pytest-xdist>=3.3.0
pytest-asyncio>=0.21.0
pytest-timeout>=2.2.0

# --- Intentionally excluded ---
# streamlit          — web UI only, never imported in src/
# ragas              — never imported in src/
# mistral-common     — never imported in src/
# fastapi, uvicorn   — API server only, no unit tests
